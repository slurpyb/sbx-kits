title = "sbx-kits secret scanning"

[extend]
useDefault = true

# These are developer-local, gitignored sandbox environment directories rather
# than repository source. Their credentials are intentionally never committed.
[[allowlists]]
description = "Ignore local sandbox environment directories"
paths = [
  '''^envs/''',
  '''^envs-rewards/''',
]
